<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    x:Class="Input_Studios_Wave.Templates.DataTemplates">
    <DataTemplate x:Key="TabHeaderTemplate">
        <Grid Height="32" Width="200">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="25"/>
                <ColumnDefinition/>
                <ColumnDefinition Width="18"/>
            </Grid.ColumnDefinitions>

            <Image Width="16" Height="16" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            <TextBlock Text="New Document" VerticalAlignment="Center" HorizontalAlignment="Left" Grid.Column="1" FontSize="14"/>
            <Button Content="x" Grid.Column="2" Padding="0" Width="16" Height="16" Background="Transparent" 
                    BorderBrush="Transparent" Foreground="White" BorderThickness="0" 
                    HorizontalAlignment="Center" VerticalAlignment="Center" Click="CloseTabButton_Click"/>
        </Grid>
    </DataTemplate>
</ResourceDictionary>
